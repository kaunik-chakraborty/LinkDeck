<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network access for on-device HTTP redirect resolution -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Package visibility: Query web-capable handlers, browsers, and share targets without requesting QUERY_ALL_PACKAGES -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:name=".LinkDeckApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LinkDeck"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <!-- Main Dashboard Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Walkthrough Activity -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false" />

        <!-- Dedicated Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />

        <!-- Comprehensive Features & Architecture Guide Activity -->
        <activity
            android:name=".ui.guide.FeaturesGuideActivity"
            android:exported="false" />

        <!-- Widget Quick Links Configuration Activity -->
        <activity
            android:name=".widget.WidgetSettingsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Diagnostic Test Link Activity -->
        <activity
            android:name=".ui.testlink.TestLinkActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Translucent Link Interception & Chooser Activity -->
        <activity
            android:name=".ui.chooser.ChooserActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.LinkDeck.Translucent">

            <!-- Primary Web Link Interception -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>

        </activity>

        <!-- Share Clean Link Interception Activity -->
        <activity
            android:name=".ui.share.ShareCleanActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.LinkDeck.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <!-- Widget Action Handler -->
        <activity
            android:name=".widget.WidgetActionActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Multi-Link & Quick Paste Widget Provider -->
        <receiver
            android:name=".widget.LinkDeckMultiLinkWidget"
            android:exported="true"
            android:label="@string/widget_multilink_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_multilink_info" />
        </receiver>

        <!-- Status Widget Provider -->
        <receiver
            android:name=".widget.LinkDeckStatusWidget"
            android:exported="true"
            android:label="@string/widget_status_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_status_info" />
        </receiver>

        <!-- Quick Settings Tile for 1-Tap Clipboard Cleaning -->
        <service
            android:name=".service.CleanClipboardTileService"
            android:icon="@drawable/ic_tile_clean_clipboard"
            android:label="@string/tile_clean_clipboard_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>

</manifest>